{
  "name": "tweetmap",
  "version": "1.0.0",
  "private": true,
  "description": "tweets on a map",
  "devDependencies": {
    "http-server": "^0.9.0",
    "bower": "^1.7.9"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hertzan/Intprog_projekt.git"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server app/ -a 0.0.0.0 -p 8000"
  }
}
